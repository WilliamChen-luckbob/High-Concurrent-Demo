spring:
  application:
    name: inventory
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: ${wstation.nacos.server-addr}
        # 配置文件后缀名为yaml
        file-extension: yaml
        namespace: ${wstation.nacos.config.namespace}
      #        group: SEATA_GROUP
      discovery:
        server-addr: ${wstation.nacos.server-addr}
#        namespace: ${wstation.nacos.config.namespace}
#        group: SEATA_GROUP
