<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.william_workstation.high_concurrent_demo.inventory.mapper.InventoryMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.william_workstation.high_concurrent_demo.inventory.entity.Inventory">
        <id column="id" property="id" />
        <result column="sku" property="sku" />
        <result column="quantity" property="quantity" />
        <result column="occupied_quantity" property="occupiedQuantity" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, sku, quantity, occupied_quantity
    </sql>

</mapper>
